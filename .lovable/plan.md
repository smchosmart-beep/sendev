문제를 잘못 이해했습니다. 첨부 화면 기준으로 보면 원하는 것은 ‘포스트잇 카드 자체가 화면 맨 아래까지 내려와 붙는 것’입니다. 지금 구조는 하단 고정 영역 안에 카드가 있지만 카드 줄 아래쪽에 실제 카드 높이보다 남는 영역/정렬 여백이 생길 수 있습니다.

수정 계획:
1. `src/components/HackathonReviews.tsx`의 모바일 후기 스트립에서 펼침 영역을 `max-height` 중심이 아니라 실제 카드 높이에 맞춘 고정 높이 컨테이너로 바꿉니다.
2. 펼쳐졌을 때 카드 행을 `items-end`로 정렬해 포스트잇 카드의 아래끝이 고정 영역의 아래끝, 즉 화면 하단에 붙게 합니다.
3. 펼침 영역 내부의 불필요한 위/아래 여백을 제거하고, 필요한 여백은 카드 위쪽에만 생기게 조정합니다.
4. 접혔을 때는 현재처럼 손잡이(“후기 1 ▲”)만 남기고, 펼쳤을 때는 손잡이 아래의 포스트잇이 화면 맨 아래까지 내려오도록 합니다.
5. 프로젝트 규칙에 따라 `/guide`의 해커톤 후기카드 설명에도 모바일 표시 방식 문구를 함께 갱신합니다.

기술 메모:
- 수정 대상: `src/components/HackathonReviews.tsx`, `src/routes/_main.guide.tsx`
- 핵심 변경: 모바일 펼침 영역을 `h-44` 또는 카드 크기와 동일한 높이로 고정하고 카드 행에 `h-full items-end`를 적용합니다.