## 변경 내용
목록에 표시되는 라벨 문구만 `(숨김)` → `(비활성)` 으로 변경합니다. 기능·로직·DB는 일절 변경하지 않습니다.

**대상 파일:** `src/routes/_main.board.index.tsx`

- 89~98행 `BoardCard` 비활성 카드: `비활성 (숨김)` → `비활성`
- 190행 `FolderNode` 폴더 라벨: `(숨김)` → `(비활성)`

관리자 화면(`눈 가림 아이콘`/"목록에서 숨김") 표현과 코드 주석은 사용자 노출 문구가 아니므로 그대로 둡니다.

## 가이드 업데이트
변경된 용어와 일관되도록 `src/routes/_main.guide.tsx`의 관련 설명에서 사용자 화면 기준 표현을 "비활성"으로 맞춥니다(관리자 측 "숨김" 용어는 유지).